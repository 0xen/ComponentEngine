#version 460

#extension GL_KHR_shader_subgroup_ballot: enable

layout(std430, set=0, binding=0) buffer layout_in_stuff {
  vec3 in_stuff[];
};
layout(std430, set=0, binding=1) buffer layout_in_scale {
  float scale;
};

void main(){
	uint GIX = gl_GlobalInvocationID.x;
	in_stuff[GIX] *=scale;
}